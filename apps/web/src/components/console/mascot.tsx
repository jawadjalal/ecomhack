"use client";

/**
 * Animated crew, shared with the cream console. SVGs live in public/mascots.
 * `MASCOT_CSS` is unused now (motion lives in each SVG, including prefers-reduced-motion).
 */
export { Mascot, useChatMascot, type MascotKind, type MascotState, type MascotProps } from "@/components/mascot/mascot";
export { DarwinLogo } from "@/components/mascot/logo";

export const MASCOT_CSS = "";
