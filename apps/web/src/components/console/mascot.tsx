/**
 * The assistant panel's mascots: the same animated team set as the rest of the app (see components/dw/mascot).
 * `MASCOT_CSS` is kept for old imports; the animated SVGs carry their own CSS.
 */
export { Mascot, type MascotKind, type MascotState } from "@/components/dw/mascot";

export const MASCOT_CSS = "";
