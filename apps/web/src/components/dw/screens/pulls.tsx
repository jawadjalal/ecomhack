"use client";

import { PageHead } from "../ui";

/** PullRequests screen (Darwin app). */
export function PullRequestsScreen() {
  return <PageHead title="PullRequests" lede="Coming together." />;
}
