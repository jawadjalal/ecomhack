import { GET as card } from "../route";

export const dynamic = "force-dynamic";

/** GET /a2a/whop/agent-card.json — the same agent card, at the path A2A clients look for. */
export const GET = card;
