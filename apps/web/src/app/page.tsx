import { Landing } from "@/components/dw/landing/landing";

/**
 * The landing page: the storefront that improves itself, for people and AI shoppers.
 * Links: Set up your store / Get started → /onboarding, Open Darwin → /console,
 * Agent readiness → /readiness, Demo store → /store.
 */
export default function Home() {
  return <Landing />;
}
